# Deprecated Playwright specs (v14–v22)

CI uses only Next specs under `frontend/e2e/` (not this folder). Vite-era v23–v27 specs live in `frontend/e2e/quarantine/`.

These specs are kept for local regression reference only.
